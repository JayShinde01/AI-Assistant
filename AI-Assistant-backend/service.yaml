apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  type: NodePort   # use LoadBalancer in cloud
  selector:
    app: fastapi-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007